const { TestWatcher } = require("jest")

describe('Pdf service', () => {
    describe('when project id is valid', () => {
        it('returns 200 when is valid response', async () => {

        })

        it('returns errors when is invalid response', async () => {

        })
    })

    describe('when project id is valid and template id is valid', () => {
        it('returns valid response', async () => {

        })

        it('returns invalid response', async () => {

        })
    })

    describe('when project id is valid and template id is invalid', () => {
        it('returns 200 status', async () => {

        })

        it('returns 404 status', async () => {

        })
    })
})